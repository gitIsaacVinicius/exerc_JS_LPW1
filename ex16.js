// 16. Dobrar Valores (map):
// Dado o array [1, 2, 3, 4, 5], use map para criar um novo array com cada número
// dobrado.

const valores = [1, 2, 3, 4, 5]

const valoresDobrados = valores.map(valor => valor * 2)

console.log(`Os valores dobrados são: [${valoresDobrados}]`)
